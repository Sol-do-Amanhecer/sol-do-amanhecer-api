spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: prjsoldoamanhecer@gmail.com
    password: qvlp jybj ijvl epbs
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls.enable: true
    default-encoding: UTF-8

cors:
  originPatterns: "http://localhost:8080, http://localhost:8081, http://localhost:8083, http://localhost:4200, http://localhost:5173"

security:
  jwt:
    token:
      secret-key: 53cr37
      expire-length: 3600000

springdoc:
  paths-to-match: /sol-do-amanhecer/api/**
  swagger-ui:
    use-root-path: true

management:
  endpoints:
    web:
      base-path: /sol-do-amanhecer/api/actuator