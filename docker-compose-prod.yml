version: "3.0"

services:
  postgres-prod-service:
    build:
      context: .
      dockerfile: ./docker-config/db/Dockerfile
    container_name: sol-do-amanhecer-postgres-prod
    volumes:
      - postgres_data_prod:/var/lib/postgresql/data
    ports:
      - ${POSTGRES_HOST_PORT}:${POSTGRES_PORT}
    networks:
      - sol-do-amanhecer-network-prod
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    restart: always

  api-prod-service:
    build:
      context: .
      dockerfile: ./docker-config/app/Dockerfile
    container_name: sol-do-amanhecer-api-prod
    depends_on:
      - postgres-prod-service
    ports:
      - ${APP_HOST_PORT}:${APP_PORT}
    networks:
      - sol-do-amanhecer-network-prod
    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE}
      APP_PORT: ${APP_PORT}
      POSTGRES_HOST: postgres-prod-service
      POSTGRES_PORT: ${POSTGRES_PORT}
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    restart: always

networks:
  sol-do-amanhecer-network-prod:

volumes:
  postgres_data_prod: